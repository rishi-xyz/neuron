{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**", "dist/**"],
      "env": ["NODE_ENV", "SLACK_BOT_TOKEN", "SLACK_APP_TOKEN"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "env": ["NODE_ENV", "SLACK_BOT_TOKEN", "SLACK_APP_TOKEN"]
    },
    "check-types": {
      "dependsOn": ["^check-types"],
      "env": ["NODE_ENV", "SLACK_BOT_TOKEN", "SLACK_APP_TOKEN"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
